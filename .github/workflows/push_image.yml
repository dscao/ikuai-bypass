name: Docker Image CI (stable versions)

on:
  push:
    tags:
      - "v[0-9]*"
      - "!v*alpha*"
      - "!v*beta*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag version to be used for Docker image"
        required: true
        default: "latest"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            dscao/ikuai-bypass
          tags: |
            type=raw,value=${{ steps.parse-tag.outputs.value }},enable=${{ github.event_name == 'workflow_dispatch' }}
            # 原始自动生成规则（仅非手动触发时生效）
            type=ref,event=branch,enable=${{ github.event_name != 'workflow_dispatch' }}
            type=ref,event=pr,enable=${{ github.event_name != 'workflow_dispatch' }}
            type=semver,pattern=v{{version}},enable=${{ github.event_name != 'workflow_dispatch' }}
            type=semver,pattern=v{{major}}.{{minor}},enable=${{ github.event_name != 'workflow_dispatch' }}

      - name: Log in to DOCKERHUB
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}


      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
